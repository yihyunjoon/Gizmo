# Gizmo config file
# Location: $XDG_CONFIG_HOME/gizmo/config.toml (fallback: ~/.config/gizmo/config.toml)

config-version = 1

[launcher]
display = "active_window" # primary | mouse | active_window
force_english_input_source = false

[launcher.global_hotkey]
key = "space"
modifiers = ["command", "shift"] # command | shift | option | control | function

[custom_menubar]
enabled = false
border = true
display_scope = "all" # all | active | primary
position = "bottom" # top | bottom
height = 30
widgets = ["front_app", "clock"] # front_app | clock
background_opacity = 0.75
horizontal_padding = 8
clock_24h = true

[workspace]
enabled = true
names = ["1", "2", "3", "4", "5"]
hide_strategy = "corner_offscreen" # corner_offscreen

[gaps]
inner.horizontal = 0
inner.vertical = 0
outer.left = 0
outer.top = 0
outer.right = 0
outer.bottom = 0

[keystats]
auto_start_monitoring = true
